// import {NodeJsonExporter} from '../Node'

// export class BaseNodeSopSubnetworkJsonExporter extends NodeJsonExporter {

// 	add_custom() {

// 		if (this._node.children_allowed && this._node.children_allowed()){
// 			const display_node = this._node.display_node()
// 			if( display_node ){
// 				if(display_node.has_display_flag()){
// 					this._data['display_node_name'] = display_node.name()
// 				}
// 			}
// 		}

// 	}

// }
