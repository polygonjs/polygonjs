// import {NodeJsonExporter} from '../Node';
// import {BaseObjNodeType} from '../../../../nodes/obj/_Base';

// export class BaseNodeObjJsonExporter extends NodeJsonExporter<BaseObjNodeType> {
// 	add_custom() {
// 		if (this._node.children_controller.children_allowed()) {
// 			// && this._node.children_allowed()){
// 			const display_node = this._node.display_node();
// 			if (display_node !== undefined) {
// 				if (display_node.has_display_flag()) {
// 					this._data['display_node_name'] = display_node.name();
// 				}
// 			}
// 		}
// 	}
// }
