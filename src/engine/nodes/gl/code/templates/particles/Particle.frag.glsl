varying vec4 vColor;

void main() {

	// float f = length( gl_PointCoord - vec2( 0.5, 0.5 ) );
	// if ( f > 0.5 ) {
	// 	discard;
	// }
	// gl_FragColor = vColor;
	gl_FragColor = vec4( 1.0, 0.0, 1.0, 1.0 );


}
