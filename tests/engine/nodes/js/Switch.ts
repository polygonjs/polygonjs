import type {QUnit} from '../../../helpers/QUnit';
export function testenginenodesjsSwitch(qUnit: QUnit) {
// tested in animationMixer
// qUnit.test('js/switch spare params are not created if the inputs are animationAction', async (assert) => {
// 	assert.equal(1, 2);
// });

}