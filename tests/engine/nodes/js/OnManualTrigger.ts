import type {QUnit} from '../../../helpers/QUnit';
export function testenginenodesjsOnManualTrigger(qUnit: QUnit) {
// no need to test here as it is used in multiple tests
// qUnit.test('js/onEventManualTrigger', async (assert) => {
// 	assert.equal(1, 2);
// });

}