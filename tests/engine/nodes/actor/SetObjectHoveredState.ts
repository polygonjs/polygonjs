QUnit.test(
	'actor/setObjectHoveredState creation and deletion has a pointermove event added/removed without the need to have a event/pointermove node',
	async (assert) => {
		assert.equal(1, 2);
	}
);
